% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulated_annealing.R
\name{allele_span_counts}
\alias{allele_span_counts}
\title{Number of Alleles Seen at Each Site or Span}
\usage{
allele_span_counts(d, s, m_default = 20, m_headroom = 5)
}
\arguments{
\item{d}{Matrix of observed alleles at the leaves (\eqn{n \times S}).}

\item{s}{Number of sites.}

\item{m_default}{Value every entry starts at.}

\item{m_headroom}{Number of unobserved alleles allowed on top of the
observed count.}
}
\value{
An \eqn{S \times S} matrix of allele counts.
}
\description{
Counts how many distinct alleles the data contains at each site and at each
span of sites cut simultaneously, and returns the matrix \code{m} of allele
counts used by the likelihood. Every entry starts at \code{m_default} and is
raised to the observed count plus \code{m_headroom} where that is larger, so
that alleles which were never observed still have some probability.
}
