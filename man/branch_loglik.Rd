% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coal_times.R
\name{branch_loglik}
\alias{branch_loglik}
\title{Log-Likelihood of the Data Given Branch Lengths}
\usage{
branch_loglik(
  pairs,
  d,
  hap,
  b,
  eigen_q,
  states,
  states_matrix,
  m,
  t_prob,
  approx = 1,
  mu = NULL
)
}
\arguments{
\item{pairs}{Matching representation of the topology (output of
\code{matching}).}

\item{d}{Matrix of observed alleles at the leaves (\eqn{n \times S}), or a
list of such matrices, one per integration barcode.}

\item{hap}{Output of \code{haplotypes} for \code{d}, or a list of such
outputs.}

\item{b}{Numeric vector of branch lengths of length \eqn{2n - 2}, in the
node labelling used by \code{pairs}.}

\item{eigen_q}{Eigendecomposition of the rate matrix, with the extra
component \code{inv} holding the inverse of the eigenvectors. May be a list,
one per integration barcode.}

\item{states}{Hash table of mutation states (output of \code{state_space}).}

\item{states_matrix}{Matrix of mutation states (output of
\code{state_space_matrix}).}

\item{m}{Symmetric matrix which stores the number of alleles per site or
site group.}

\item{t_prob}{Transition probability matrix over one time unit. May be a
list, one per integration barcode.}

\item{approx}{Set to \code{0} for the exact likelihood and \code{1} for the
approximate likelihood which only considers alleles seen at the leaves.}

\item{mu}{Optional list of conditional mutation probabilities, one per
integration barcode. If \code{NULL} a uniform distribution is assumed.}
}
\value{
The log-likelihood, summed over integration barcodes.
}
\description{
Computes the log-likelihood of the observed allele data on a tree with a
fixed topology and fixed branch lengths. When several independent
integration barcodes are observed on the same tree, the log-likelihoods are
summed.
}
