% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coal_times.R
\name{gradient_branches}
\alias{gradient_branches}
\title{Gradient of the Log-Likelihood with Respect to Branch Lengths}
\usage{
gradient_branches(p, q, q_allele)
}
\arguments{
\item{p}{Forward probability matrix (second element of the output of
\code{pq_vectors}).}

\item{q}{Backward probability matrix (third element of the output of
\code{pq_vectors}).}

\item{q_allele}{Rate matrix restricted to the observed alleles (output of
\code{q_alleles}).}
}
\value{
A numeric vector of length \eqn{2n - 2}, where entry \eqn{j} is the
derivative of the log-likelihood with respect to the branch which ends at
node \eqn{j}.
}
\description{
Returns the vector of gradients of the log-likelihood with respect to the
branch lengths of the tree. \code{p} and \code{q} are the forward and
backward probability matrices returned by \code{pq_vectors}: each column
corresponds to a node, and the last column corresponds to the root, which
has no branch above it and is therefore dropped.
}
\details{
This is a direct port of \code{gradient()} in
\code{new_code/new_functions.R}.
}
